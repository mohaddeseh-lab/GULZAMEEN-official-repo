import { S3Client, ListObjectsV2Command, GetObjectCommand, PutObjectCommand } from '@aws-sdk/client-s3'; const r2 = new S3Client({ region: 'auto', endpoint: `https://${process.env.R2_ACCOUNT_ID}.r2.cloudflarestorage.com`, credentials: { accessKeyId: process.env.R2_ACCESS_KEY_ID, secretAccessKey: process.env.R2_SECRET_ACCESS_KEY, }, }); const aws = new S3Client({ region: process.env.AWS_REGION, credentials: { accessKeyId: process.env.AWS_ACCESS_KEY_ID, secretAccessKey: process.env.AWS_SECRET_ACCESS_KEY, }, }); export async function GET() { const list = await r2.send(new ListObjectsV2Command({ Bucket: process.env.R2_BUCKET_NAME })); const objects = list.Contents || []; let copied = 0; for (const obj of objects) { const file = await r2.send(new GetObjectCommand({ Bucket: process.env.R2_BUCKET_NAME, Key: obj.Key })); const bytes = await file.Body.transformToByteArray(); await aws.send(new PutObjectCommand({ Bucket: process.env.AWS_BACKUP_BUCKET, Key: obj.Key, Body: bytes })); copied = copied + 1; } return Response.json({ backedUp: copied }); }